<!DOCTYPE html>
<html lang="ru">
  <head>
    <%= display_meta_tags site: "Startap", reverse: true %>
    <%= favicon_link_tag 'icon.svg' %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "mobile_menubar" %>
    <div class="S_mainContainer">
      <%= render "menubar" %>
      
      <%#= render "devise/shared/links" %>
      <%= yield %>


    </div>

    <%= render "footer" %>
  </body>
</html>
